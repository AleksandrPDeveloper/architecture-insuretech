apiVersion: v1
kind: Service
metadata:
  name: scaletestapp
spec:
  type: LoadBalancer
  selector:
    app: scaletestapp
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080